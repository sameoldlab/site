<header>
	<a href='/' aria-label='Homepage' title='home'> ibro.xyz</a>

	<nav class='item' data-sveltekit-preload-data='hover'>
		<a href='/lab'>lab</a>
		<a href='/now'>now</a>
		<a href='/notes'> notes</a>
	</nav>
</header>

<style>
	header {
		/* fixed px-4 md:px-0 py-2 left-0 right-0 bottom-0 text-lg z-10 transition-all duration-1000 ease-in-out */
		position: fixed;
		bottom: 0;
		left: 0;
		right: 0;
		z-index: 12;
		background-color: var(--bg-low);
		display: flex;
		backdrop-filter: blur(4px);
		/* opacity: .58; */
		mask-image: linear-gradient(transparent, var(--bg-low) 45%);
		align-items: center;
		justify-content: space-between;
		padding: 0.75em 1.75em 0.25em;
		& > a {
			font-style: italic;
		}
		nav {
			display: flex;
			gap: 2em;
		}
	}
</style>
